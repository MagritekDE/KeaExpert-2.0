######################################
# optimised load times (per load)
#                ~ 700us for 1D plot
#                ~ 60ms for 2D plot
#                ~ 900us for macro
# V2.2.5
######################################

time(0)
cd(getmacropath())
for(k = 0 to 10000)
load("data.mac")
#load("data.pt1")
#load("data.pt2")
next(k)
pr time()

