procedure(getprocnamesTest)

   path = "$appdir$\\Macros\\NMRI"
   file = "importData.mac"

   (procs,args) = getprocnames(path,file)
   
   for(k = 0 to size(procs)-1)
      print("\n$procs[k]$($args[k]$)")
   next(k)

endproc()