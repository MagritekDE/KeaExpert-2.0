procedure(Pump controller)

   n = :windowdefinition()
   showwindow(n)

endproc()

procedure(windowdefinition)

   # Automatically generated window definition procedure.
   # Any code added manually will be removed if layout modified interactively
   n = window("Pump controler", -1, -1, 278, 169, "resizable")

      # Define all controls with basic parameters
      checkbox(1, 75, 68, "off,on", "off")
      statictext(2, 61, 67, "right", "Pump on")
      statictext(3, 125, 57, "right", "cw")
      radiobuttons(4, 139, 57, 20, "vertical", "cw,ccw", "cw")
      statictext(5, 126, 76, "right", "ccw")
      textbox(6, 70, 110, 41)
      statictext(7, 62, 113, "right", "Speed")
      statictext(8, 138, 113, "right", "rpm")
      button(10, 199, 18, 47, 28, "Close")
      textmenu(11, 70, 20, 51, 200)
      statictext(12, 62, 23, "right", "Port")

     # Set other control parameters
      setpar(n,6,"valueID","speed")
      setpar(n,11,"valueID","port",
                  "menu",[""])

     # Set other window parameters
endproc(n)