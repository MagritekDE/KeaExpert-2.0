procedure(uniquename_test1,verbose,delay)

   m = 12

   if(uniquename("m") != "m1")
      return(0, "Invalid result for: uniquename_test1 #1")
   endif 

   m1 = m

   if(uniquename("m") != "m2")
      return(0, "Invalid result for: uniquename_test1 #2")
   endif 

endproc(1,"")
