procedure(lasterror_test1,verbose,delay)

   for k = -5 to 5
      try
         v = 1/k
      catch
         if(lasterror() != "divide by zero")
           return(0, "Invalid result for lasterror_test1")
         endif
      endtry
   next k


endproc(1,"")



