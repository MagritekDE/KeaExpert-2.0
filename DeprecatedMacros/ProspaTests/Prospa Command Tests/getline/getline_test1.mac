procedure(getline_test1,verbose,delay)

  cd(getmacropath())
  txt = load("test.txt")
  line = getline(txt,108)

  if(line != "When the moon is a ghostly galleon tossed upon cloudy seas,")
     return(0, "Invalid result for: getline_test1() #1")
  endif

endproc(1,"")