procedure(scale3d_test1,verbose=0,delay=0.01)

   try
      m3d = load("$appdir$\\Example Data\\native\\small.3d")
      clear3d()
      isosurf(m3d,30000,[.8,.2,0],[-20,20,-20,20,-20,20])
      for(x = 0.5 to 4 step 0.05)
        scale3d(x,x,x)
        pause(delay)
      next(x)
   catch
      pr lasterror() 
   endtry

endproc(1,"")
