procedure(catch_test1,verbose,delay)

   for k = -10 to 10
      try
         v = 1/k
      catch
         if(k == 0)
            pr "      Valid test: catch_test1\n"
            return(1,"")
         endif
      endtry
   next k

endproc(0, "Invalid result for: catch_test1")
