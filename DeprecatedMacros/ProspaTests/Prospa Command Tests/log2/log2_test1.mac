procedure(log2_test1,verbose,delay)

   k = [-100:100]
   p = 2^k
   if(log2(p) != k)
      return(0, "Invalid result for: log2_test1")
   endif 

   for(k = -1 to -100)
      if(log2(k) != nan)
         return(0, "Invalid result for: log2_test1")
      endif 
   next(k)

endproc(1,"")
