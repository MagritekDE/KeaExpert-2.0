procedure(edit_test1,verbose,delay)

   txt = "This is a small test of the\nedit function. To see\nif I can save some text\nand then reload it."
   save("temp.txt",txt)
   clear("edit")
   edit("temp.txt")
   pause(delay)
   rmfile("temp.txt")

endproc(1,"")
