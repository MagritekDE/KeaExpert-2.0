procedure(ift_test1,verbose,delay)

   (x,y) = complex(1024,1,10,0.05)
    d = (ift(ft(y)) - y)
   if(max(real(d)) > 1e-6*max(real(y)))
        return(0, "Invalid result for ift_test")
    endif

endproc(1,"")