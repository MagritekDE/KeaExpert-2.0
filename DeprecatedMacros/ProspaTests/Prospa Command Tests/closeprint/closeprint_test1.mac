procedure(closeprint_test1,verbose,delay)

#  printtofile("temp")
#  for(m = 0 to 10)
#    pr("$m$\n")
#  next(m)
#  closeprint
#
#  lst = load("temp","list")
#  for(m = 0 to 10)
#    if(lst[m] != "$m$")
#       abort("Invalid result for closeprint")
#     endif
#  next(m)
#
#  rmfile("temp")
  
    
endproc(1,"")
