procedure(ispar_test1,verbose,delay)

  lst = ["a = 23","b = 45","c = 67"]

  if(ispar(lst,"c") != 1)
     return(0, "Invalid result for ispar_test1 #1")
  endif

  if(ispar(lst,"q") != 0)
     return(0, "Invalid result for ispar_test1 #2")
  endif

endproc(1,"")



