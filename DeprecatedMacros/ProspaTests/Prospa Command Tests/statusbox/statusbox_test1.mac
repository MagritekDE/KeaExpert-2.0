procedure(statusbox_test1,verbose,delay)

   n = window("statusbox test",200,100,200,100)
   statusbox(1)
   showwindow(n)
   
   for(k = 0 to 100)
     setpar(n,1,"text","n = $k$")
     pause(0.02)
   next(k)

   closewindow(n)

endproc(1,"")
