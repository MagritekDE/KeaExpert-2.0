procedure(simplifydir_test1,verbose,delay)

    dir = "C:\\Documents and Settings\\..\\Documents and Settings\\..\\"
    if(simplifydir(dir) != "C:\\")
      return(0, "Invalid result for: #1")
    endif 

endproc(1,"")
