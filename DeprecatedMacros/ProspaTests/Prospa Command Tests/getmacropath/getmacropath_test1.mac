procedure(getmacropath_test1,verbose,delay)

  dir =  getcwd()
  path = getmacropath()

  if(caseset(dir,"lower") != caseset(path,"lower"))
     return(0, "Invalid result for: getmacropath_test1() #1")
  endif

endproc(1,"")