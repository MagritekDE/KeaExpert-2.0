#################################################################
# Set whether the start and end delays will be used when running
# an experiment via the RunExpt command
#################################################################

procedure(UseDelays, start=0, end=0)

   gData->curExpt->parameters->useStartDelay = start
   gData->curExpt->parameters->useEndDelay = end

endproc()
