procedure(EditIfShiftPressed, macPath ,macName)

   if(iskeypressed("shift"))
      if(gData->permissions->canEditScripts == "yes")
         bak = getcwd()
         EditorWin()
         cd(macPath)
         ed(macName)
         cd(bak)
      endif
      return(1)
   endif

endproc(0)