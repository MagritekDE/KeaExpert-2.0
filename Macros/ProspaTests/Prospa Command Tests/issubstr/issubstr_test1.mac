procedure(issubstr_test1,verbose,delay)

  str = "This is a test"

  if(issubstr(str,"is") != 1)
     return(0, "Invalid result for issubstr_test1 #1")
  endif

  if(issubstr(str,"q") != 0)
     return(0, "Invalid result for issubstr_test1 #2")
  endif

endproc(1,"")



