# Take the ones complement of a range of numbers

procedure(inv_test1,verbose,delay)

    for(k = -1000 to 1000)
      if(inv(k) != -k-1)
          return(0, "Invalid result for: inv_test1")
      endif
    next(k)
       
endproc(1,"")
