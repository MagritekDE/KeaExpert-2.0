

procedure(relpath_test1,verbose,delay)

   dir = getcwd()
   if(relpath(dir,appdir) != "$$appdir$$\\Prospa Tests\\relpath\\")
      return(0, "Invalid result for: relpath_test1")
   endif 
 
endproc(1,"")
