procedure(loge_test1,verbose,delay)

   e = exp(1)
   k = [-80:80]
   p = e^k
   if(loge(p) != k)
      return(0, "Invalid result for: loge_test1")
   endif 

   for(k = -1 to -80)
      if(loge(k) != nan)
         return(0, "Invalid result for: loge_test1")
      endif 
   next(k)

endproc(1,"")
