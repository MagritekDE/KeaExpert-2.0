procedure(zoom2d_test1,verbose,delay)

   m = load("$appdir$\\Example Data\\native\\orange.2d")
   image(m)
   (w,h)= size(m)
   for(k = 0 to 240)
     zoom2d(k,w-k-1,k,w-k-1)
   next(k)

endproc(1,"")
