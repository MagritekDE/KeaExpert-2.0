procedure(log10_test1,verbose,delay)

   k = [-38:38]
   p = 10^k
   if(log10(p) != k)
      return(0, "Invalid result for: log10_test1")
   endif 

   for(k = -1 to -38)
      if(log10(k) != nan)
         return(0, "Invalid result for: log10_test1")
      endif 
   next(k)

endproc(1,"")
