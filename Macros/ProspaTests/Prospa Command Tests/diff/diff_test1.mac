procedure(diff_test1,verbose,delay)

   clear1d()
   t = [0:100]
   s = sin(t/10)
   plot(s,"tracecolor",[255,255,0])
   hold("on")
   plot(diff(s/0.1),"tracecolor",[255,0,0])
   hold("off")
   title("sine function and its approx differential")
   pause(delay)

  
endproc(1,"")
