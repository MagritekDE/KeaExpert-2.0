# Findindex function test
procedure(findindex_test1,verbose,delay)

  x = [0:0.1:1]
  (in1,in2,in3) = findindex(x,0.22,0.49,0.65)
  if(in1 != 2 | in2 != 5 | in3 != 7)
       return(0, "Invalid result for: findindex") 
  endif

endproc(1,"")
