procedure(testArgNames)

   time(0)
   for(k = 0 to 999999)
   testArgNames:func(p3=1,
                     p1=5,
                     p2=6,
                     p4=7)
  # :func(5,6,3)
   next(k)
   print(time())

endproc()

procedure(func,p1,
               p2,p3,p4=0)

 # if(nrArgs == 3)
 #     p4 = 0
 #  endif

endproc()
