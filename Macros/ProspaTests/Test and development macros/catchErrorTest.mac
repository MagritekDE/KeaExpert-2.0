procedure(catchErrorTest)

   try
   
     for(k = 0 to 1000)
   
        :func(k) 
   
     next(k)
   
   catch
   
     pr lasterror()
   
   endtry

endproc()

procedure(func,q)

  if(q == 500)
    dfds
  else
   pr("q = $q$\n")
  endif

endproc()
