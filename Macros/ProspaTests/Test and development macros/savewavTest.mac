procedure(savewavTest)

  t = linspace(0,2,20000)
  f1 = 200
  f2 = 205
  y = cos(2*pi*f1*t).*exp(-t*2)
  y = y+cos(2*pi*f2*t).*exp(-t*2)
  plot(t,y)
  savewav("$desktop$\\test.wav",y*15000,10000)
  sound(real(y),10000)

endproc()
