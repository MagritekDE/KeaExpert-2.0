x = [0:0.001:20]
stddev = 5
amp = 5
pos = -10
ns = 0.1
y = amp*exp(-(x+pos)^2/(2*stddev^2))
y = y + noise(size(x))*ns
plot(x,y,"color",[128,0,0])

fit = gaussfit(x,y)

hold("on")
plot(x,fit,"color",[0,128,0])
#
hold("off")