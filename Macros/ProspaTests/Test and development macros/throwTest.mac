procedure(throwTest)

   try
      
   for(k = -10 to 10)
   
      if(k == 0)
         throw("divide by zero")
      endif
      pr "k = $1/k$\n"
   
   next(k)
   
   catch

     print("Caught\n")
     err = lasterror()
     print(err->description)

   
   endtry

endproc()