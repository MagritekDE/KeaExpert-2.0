###################################################################################
#######################################################

procedure(endiftrue, skipName)

   cnt = gSeq->psInfo->loopCnt
   gSeq->psInfo->durationNStk[cnt] = gSeq->psInfo->durationNStk[cnt]
   gSeq->psInfo->command = gSeq->psInfo->command + ["endiftrue,$skipName$"]
   gSeq->psInfo->skipCmds = 0

endproc()

