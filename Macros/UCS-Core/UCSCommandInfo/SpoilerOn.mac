procedure(SpoilerOn, x0, y0, z0)

   if(gSeq->psInfo->skipCmds == 0)

      args = getargnames()
      cnt = gSeq->psInfo->loopCnt
      totDur = 2.5*3
      gSeq->psInfo->durationNStk[cnt] = gSeq->psInfo->durationNStk[cnt] + totDur
      gSeq->psInfo->command = gSeq->psInfo->command + ["spoileron,xyz,$totDur$"]

   endif

endproc()


