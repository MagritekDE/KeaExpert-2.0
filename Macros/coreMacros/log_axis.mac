# Generate a logarithmically spaced axis between
# mn and mx with N points

procedure(log_axis,mn,mx,N)

   step = (log10(mx)-log10(mn))/(N-1)
   x = [log10(mn): step: log10(mx)]
   xl = 10^x

endproc(xl)