if(EditIfShiftPressed(getmacropath(),getmacroname()))
   return
endif 

pr ("\n   --- Pulse sequence examples ---\n")
pr ("\n   These macros demonstrate the use of some of the more complex\n")
pr ("   pulse program commands. Note that care should be taken when\n")
pr ("   executing these not to leave RF, shim or gradients on for an\n")
pr ("   excessive amount of time.\n")
    
 